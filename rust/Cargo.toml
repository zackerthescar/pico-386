[package]
name = "pico386-rs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
peg = { version = "0.8.5", default-features = false }

[patch.crates-io]
peg = { path = "patches/peg-0.8.5" }

[profile.release]
opt-level = 2
panic = "abort"
lto = true
